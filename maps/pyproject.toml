[project]
name = "gametl-map-transform"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "nudged>=0.3",
  "numpy>=2.0",
  "opencv-python-headless>=4.10",
  "pyyaml>=6.0",
  "requests>=2.31",
]

