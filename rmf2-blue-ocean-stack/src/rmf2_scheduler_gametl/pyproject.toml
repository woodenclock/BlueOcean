[project]
name = "rmf2-scheduler-stack"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "blue_ocean_scheduler",
    "demo_routes",
]

[tool.uv.sources]
demo-routes = { workspace = true }
